[Unit]
Requires=beanmap-private-transport.service
After=beanmap-private-transport.service

[Service]
# Append this check; do not clear other existing Docker ExecStartPost guards.
ExecStartPost=/usr/local/sbin/beanmap-private-transport check
