# Install as /etc/systemd/system/docker.service.d/beanmap-container-boundary.conf
# Reinstalls the guard after every Docker start; a failure fails Docker startup.
[Service]
ExecStartPost=/usr/bin/python3 /usr/local/sbin/beanmap-container-boundary --apply
