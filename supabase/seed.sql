-- Seed file: runs after migrations on `supabase db reset`
-- Origin presets are already seeded in 00004 migration.
-- Add test data here for local development if needed.

-- Example: create a test user profile (only works with local dev auth)
-- insert into auth.users (id, email, encrypted_password, email_confirmed_at, raw_app_meta_data, raw_user_meta_data, created_at, updated_at)
-- values ('00000000-0000-0000-0000-000000000001', 'test@beanlog.dev', crypt('password123', gen_salt('bf')), now(), '{"provider":"email","providers":["email"]}', '{"display_name":"테스터"}', now(), now());
